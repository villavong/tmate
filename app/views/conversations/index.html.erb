
<% if(!user_signed_in?) %>

<% else %>
  <% if current_user.revisers.present? %>

  <% end %>

<% end %>
<br>
<br>
<div class="container3">
<!-- <p><%= link_to 'Start conversation', new_message_path, class: 'btn btn-lg btn-primary' %></p> -->

  <div class="row">
    <div class="col s12 l2">
      <ul class="nav nav-pills nav-stacked " >
        <span class="label label-danger pull-left clearfix">Unread : <%=unread_messages_count%></span>
        <br>
        <%= mailbox_section 'inbox', @box, class: "teal darken-3 waves-effect waves-light btn hoverable", style: "margin: .5rem 0;" %>
        <%= mailbox_section 'sent', @box, class: "teal darken-3 waves-effect waves-light btn hoverable", style: "margin: .5rem 0;" %>
        <%= mailbox_section 'trash', @box, class: "teal darken-3 waves-effect waves-light btn hoverable" , style: "margin: .5rem 0;" %>

      </ul>
    </div>

    <div class="col s12 l8">
      <!-- <% if @box == 'trash' %>
        <p><%= link_to 'Empty trash', empty_trash_conversations_path, class: 'btn btn-danger', method: :delete, data: {confirm: 'Are you sure?'} %></p>
      <% end %> -->
      <%= will_paginate %>

      <ul class="list-group">
        <%= render partial: 'conversations/conversation', collection: @conversations %>
      </ul>

    </div>
    <div class="col s12 l2"></div>

  </div>
</div>
